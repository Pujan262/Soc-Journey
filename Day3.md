Day 3 – IP addresses,Subnet and Ports

1.IP Address -
A unique address  assigned to devices on the network.There is two category of ip address-

-Public IP Address -
It is used to communicate outside the Local Area Network(LAN) over the internet.It’s ip address is typically assigned by the ISP provider.

Private IP Address -
It is used to comminucate within local area network.It enables devices such as, computer mobile and printer to exchange data internally.

Private ip are those ip that are assigned to devices we own and without public ip address such as modem or router we cannot connect to internet.

And there are two type of ip address

Ipv4 -
-32bit numeric labels written in decimal format  allowing about 4.3billions address
Decimal value is 0 to 15

Ipv6 -
-128bit of alpha numeric labels written in hexadecimal format aloowing upto limitless number of unique ip address.
Hexadeximal value  is 0 to 9 and A to F is 10 to 15

2.Subnet Mask -
A subnet mask tell your device which part of the ip address is network and which part is host address.

It is a 32bit number used in ipv4 netwroking that divides into two parts, the network and the host address

Ports

Port21-FTP(File Transfer Protocol)
A set of rules or conventions that governs how a file are transfarred between aclient and server.

Port22-SSH(Secure Shell)
It is a protocol for connecting computer remotely securely,in a encryption channel.

Port23-Telnet
Teletype network is protol for connecting remote server, isn’t secured as ssh and anayone can etesdrop

Port25-SMPT(Simple Mail Transfer Protocol)
It is a protocol for sending email between mail server.

Port 53-DNS(Domain name System)
It maps human readable or host name like google.com to an ip address in server

Port80-HTTP(HyperText Tranfer Protocol)
It defines how a message should be structured and how data exchanges are organized between webbrowser and server

Port443- HTTPS(HyperText Tranfer Protocol Secure)
It is just like HTTP but more secured.

Port 445-SMB(Server Message Block)
It is used for sharing files of the computer that are in same netwrok along with printer.

Port3389-RDP(Remote Desktop Protocol)
The protocol that let you connct to and  controll your desktop or pc from another devices over a network


Port3306- My Sql
It is a database to communicate,manage,store and manipulate sql(Structured Query Lanuage)
